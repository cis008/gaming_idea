Place your background voice clip here as:

- `pokemon-bg-voice.mp3`

The `PrerecordedLectures` page loads this file from `/audio/pokemon-bg-voice.mp3`.
If the file is missing, browser playback will fail silently and the page remains usable.
